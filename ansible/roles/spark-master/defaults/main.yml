install:
  type: archive
  module: spark
  version: 1.6.3
  user: spark
  group: spark
  port: "{{ global.ports.spark_master_ui }}"
  file: spark-1.6.3/spark-1.6.3-bin-hadoop2.6.tgz
  site: site-spark.sh
  cfg:
    - spark-env.sh
    - log4j.properties
  services:
    - spark-master
  checks:
    port: yes
    
