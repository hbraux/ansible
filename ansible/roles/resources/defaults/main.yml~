resources:
  kafka:
    partitions: 2
    replication: "{{ groups['kakfa'] | length }}"
    topics:
      - topic1
  elasticsearch:
    shards: 1
    replicas: "{{ groups['elasticsearch'] | length -1 }}"
    indexes:
      - index1



