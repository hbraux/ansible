resources:
  kafka:
    partitions: 2
    replication: "{{ global.servers.kafka | length }}"
    topics:
      - topic1
  elasticsearch:
    shards: 1
    replicas: "{{ global.servers.elasticsearch | length -1 }}"
    indexes:
      - index1



